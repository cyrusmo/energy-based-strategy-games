"""Experiment runner and metrics helpers.

Import from submodules (for example ``strategy_games.experiments.baselines``) rather than
this package namespace to avoid import cycles during test collection.
"""
