"""Training loops and baseline algorithms.

Import from submodules (for example ``strategy_games.training.ppo_baseline``) rather than
this package namespace to avoid import cycles during test collection.
"""
